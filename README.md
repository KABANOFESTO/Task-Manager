# Task Manager Frontend

This is the frontend application for the Task Manager project, built with React.js. The application provides an intuitive interface for users to manage tasks effectively.

## Features

- User authentication (login and signup).
- Create, read, update, and delete (CRUD) tasks.
- Mark tasks as complete or incomplete.
- Filter tasks by status (all, completed, incomplete).
- Responsive design for mobile and desktop users.

---

## Tech Stack

- **Frontend Framework:** React.js
- **State Management:** Context API / Redux (if applicable)
- **Styling:** CSS / TailwindCSS / Material-UI (replace with your choice)
- **HTTP Requests:** Axios / Fetch API
- **Routing:** React Router

---

## Getting Started

### Prerequisites

Make sure you have the following installed:

- Node.js (v14 or later)
- npm or yarn package manager

### Installation

1. Clone the repository:

   ```bash
   git clone https://github.com/KABANOFESTO/Task-Manager.git
   cd taskManager
